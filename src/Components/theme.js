// src/theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: "Roboto, Arial, sans-serif", // Set the font family to Google Font
  },
  // Optionally, add custom theme settings
});

export default theme;
